FROM node:24-alpine

WORKDIR /app

ENV NEXT_TELEMETRY_DISABLED=1 \
    PORT=3034 \
    HOSTNAME=0.0.0.0

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 3034

CMD ["npm", "run", "dev", "--", "--hostname", "0.0.0.0"]
